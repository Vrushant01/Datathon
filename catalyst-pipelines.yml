version: 1

runners:
  high:
    config-id: 3

pipeline-when:
  equal:
    - << event.ref >>
    - refs/heads/main

images:
  Node20:
    image: node:20-bookworm

jobs:

  backend-build:
    cwd: /catalyst/backend
    steps:
      - node --version
      - npm --version
      - npm ci
      - npm run build

  backend-deploy:
    when:
      equal:
        - << status.build.backend-build >>
        - success
    cwd: /catalyst
    steps:
      - npm install -g zcatalyst-cli@beta
      - >-
        catalyst deploy appsail
        --name backend
        --build-path "/catalyst/backend"
        --stack "NodeJS 20"
        --command "node dist/app.js"
        --token "$CATALYST_TOKEN"
        --verbose

stages:

  - name: build
    image: Node20
    jobs:
      - backend-build

  - name: deploy
    image: Node20
    jobs:
      - backend-deploy