import { CloudScaleRepository } from './src/repositories/CloudScaleRepository'; async function main() { const db = new CloudScaleRepository(); const cases = await db.getAllCases(); console.log('Total Cases: ' + cases.length); const solvedCases = cases.filter((c: any) => c.CaseStatusID === 2 || c.CaseStatusID === 3 || c.CaseStatusID === 4).length; console.log('Solved Cases: ' + solvedCases); console.log('Pending Cases: ' + (cases.length - solvedCases)); } main();
